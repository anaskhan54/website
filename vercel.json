{
  "version": 2,
  "framework": "create-react-app",
  "buildCommand": "CI=false NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
  "installCommand": "npm install --legacy-peer-deps --force"
}
